Document helper scripts.
Add support for creating new systems (using config and helper scripts).
Send ARP request for NICs without registered IP address (e.g. for cases where guest didn't ask DHCP for IP)